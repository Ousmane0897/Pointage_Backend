package com.example.Pointage_Cleanic.Enum;

public enum TypeMouvement {

    ENTREE, // entrée en stock (réception, retour fournisseur...)
    SORTIE, // sortie (vente, casse, consommation interne...)
    AJUSTEMENT // correction lors d'inventaire (valeur fixée)
}
